# Anica Export Choices

Source of truth: `anica/src/core/export.rs` (`ExportPreset`, `ExportSettings`).

## Export Modes

1. `smart_universal` (recommended)
   - Try `Keep Source (Copy)` first.
   - Auto-fallback to preset re-encode when timeline/output requires rendering.
2. `keep_source_copy`
   - Stream copy trim only (`-c copy`).
   - Works only for simple single-source timeline with no effects/overlays/mix.
3. `preset_reencode`
   - Always use selected preset encoder pipeline (same as previous exporter behavior).

## Current UI Order (`ExportPreset::all_for_ui`)

1. `h264_mp4`
2. `h264_videotoolbox_mp4`
3. `hevc_mp4`
4. `gif`
5. `vp9_webm`
6. `vp8_webm`
7. `av1_libaom_mkv`
8. `av1_svt_mkv`
9. `dnxhr_hq_mov`
10. `prores_422_mov`
11. `prores_422_hq_mov`
12. `prores_444_mov`
13. `prores_4444_mov`
14. `aac_m4a`
15. `mp3`
16. `opus`
17. `flac`
18. `wav_pcm`

## Video Presets

| ID | Label | Container | Video Codec | Audio Codec | Pixel Format | CRF | Audio-only |
|---|---|---|---|---|---|---|---|
| `h264_mp4` | H.264 MP4 | mp4 | `h264_videotoolbox` on macOS, `libopenh264` elsewhere | `aac` | `yuv420p` | No | No |
| `h264_videotoolbox_mp4` | H.264 MP4 (VideoToolbox, macOS) | mp4 | `h264_videotoolbox` | `aac` | `yuv420p` | No | No |
| `hevc_mp4` | HEVC MP4 | mp4 | `hevc_videotoolbox` on macOS, H.264 fallback elsewhere | `aac` | `yuv420p` | No | No |
| `gif` | Animated GIF | gif | FFmpeg `palettegen` + `paletteuse` | None | palette GIF | No | No |
| `vp8_webm` | VP8 WEBM | webm | `libvpx` | `libopus` | `yuv420p` | Yes | No |
| `vp9_webm` | VP9 WEBM | webm | `libvpx-vp9` | `libopus` | `yuv420p` | Yes | No |
| `av1_libaom_mkv` | AV1 (libaom+opus) MKV | mkv | `libaom-av1` | `libopus` | `yuv420p10le` | Yes | No |
| `av1_svt_mkv` | SVT-AV1 (libsvtav1+opus) MKV | mkv | `libsvtav1` | `libopus` | `yuv420p10le` | Yes | No |
| `dnxhr_hq_mov` | DNxHR-HQ MOV | mov | `dnxhd` (`dnxhr_hq`) | `pcm_s24le` | `yuv422p10le` | No | No |
| `prores_422_mov` | ProRes 422 MOV | mov | `prores_ks` (`profile 2`) | `pcm_s16le` | `yuv422p10le` | No | No |
| `prores_422_hq_mov` | ProRes 422 HQ MOV | mov | `prores_ks` (`profile 3`) | `pcm_s16le` | `yuv422p10le` | No | No |
| `prores_444_mov` | ProRes 444 MOV | mov | `prores_ks` (`profile 4`) | `pcm_s16le` | `yuv444p10le` | No | No |
| `prores_4444_mov` | ProRes 4444 MOV | mov | `prores_ks` (`profile 4`) | `pcm_s16le` | `yuva444p10le` | No | No |

## Audio-only Presets

| ID | Label | Container | Args | CRF | Audio-only |
|---|---|---|---|---|---|
| `aac_m4a` | Audio AAC M4A | m4a | `-vn -c:a aac -b:a <kbps>` | No | Yes |
| `mp3` | Audio MP3 | mp3 | `-vn -c:a libmp3lame -b:a <kbps>` | No | Yes |
| `opus` | Audio OPUS | opus | `-vn -c:a libopus -b:a <kbps>` | No | Yes |
| `flac` | Audio FLAC | flac | `-vn -c:a flac` | No | Yes |
| `wav_pcm` | Audio WAV PCM | wav | `-vn -c:a pcm_s16le` | No | Yes |

## ExportSettings Normalization

- `fps`: clamp `1..=144`
- `crf`: clamp `0..=51`
- `audio_bitrate_kbps`: clamp `64..=512`
- `encoder_preset`: allowed
  - `ultrafast`, `superfast`, `veryfast`, `faster`, `fast`, `medium`, `slow`, `slower`, `veryslow`
  - fallback: `medium`

## Recommendation Guideline (for ACP Agent)

When user asks "export same quality/codec as source":

1. Call `anica.media_pool/list_metadata` first.
2. Read source fields: `video_codec`, `audio_codec`, `fps`, `width`, `height`, `media_kind`.
3. Map to closest Anica preset:
   - Source H.264 8-bit -> `h264_mp4`
   - Source HEVC/H.265 -> `hevc_mp4`
   - Source VP9 -> `vp9_webm`
   - Source ProRes 422/HQ -> `prores_422_mov` / `prores_422_hq_mov`
   - Source ProRes 444/4444 -> `prores_444_mov` / `prores_4444_mov`
   - Short silent social/preview animation -> `gif`
   - Audio-only source -> choose from audio-only presets.
4. If exact codec/profile is unavailable, provide "closest visually similar" option and a "max compatibility" fallback (`h264_mp4`).
5. Be explicit about tradeoff: compatibility vs file size vs edit-master quality.

## Notes

- CRF-based presets are generally distribution outputs.
- DNxHR/ProRes are mezzanine/edit-master outputs.
- For strict alpha workflows, use `prores_4444_mov`.
- GIF export omits audio and always renders through the palette pipeline; keep ranges short to avoid large files.
